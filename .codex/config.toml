[features]
multi_agent = true

[agents.luna_reader]
description = "Read-only Luna agent for bounded repository exploration, tracing, evidence gathering, and concise summaries."
config_file = ".codex/agents/luna_reader.toml"

[agents.luna_worker]
description = "Luna implementation agent for bounded changes with explicit file ownership and acceptance criteria."
config_file = ".codex/agents/luna_worker.toml"
