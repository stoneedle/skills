# 本次抓包没有 system 原文

会话 `92b4e9cb-ebc1-4702-8bae-f45bb1748d06`，record `10`，模型 `gpt-6-astra`。

请求 input 中没有 role=system 的消息；此文件是缺失说明，不是 system prompt 原文。
顶层 instructions 字段是否存在见 manifest.json。只能说明这个客户端抓包边界的情况，不能推断服务端没有额外指令。
