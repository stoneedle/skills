## 受控多 agent 委派

- 用户已持续授权主控自主委派：简单任务由主控直接完成；仅当 `luna_reader` 或 `luna_worker` 能减少主控上下文负担、形成真实并行或独立完成边界清晰的交付时才派发。当前任务中用户明确的“不使用子 agent”等限制优先。
- 主控保留需求拆分、跨模块判断、关键设计、金额/权限/删除/迁移/关键一致性决策、集成与最终验收。Luna Max 不可用时，报告限制；不得静默改用 Sol/Astra 或降低 reasoning effort。
- `luna_reader` 负责有明确问题的只读探查、调用链/日志/资料取证和短摘要；`luna_worker` 负责文件归属、修改范围和验收条件明确的较大实现、机械修改与针对性测试。每个任务一次给全背景、范围、约束、文件 owner、验收条件及 DONE/BLOCKED/FAILED 返回条件；worker 不再派生子 agent。
- 独立 V1 子任务使用 `fork_context = false`，只传必要背景；同一模块的紧密追问才复用原 agent。不同 worker 不并发写同一文件或资源；共享 checkout 时由主控协调 Git 操作。
- 主控做完不重复的工作后，用 V1 `wait_agent` 长等待；多目标是 wait-any，后续等待移除已结束目标。正常期间不轮询状态、日志或进度，不催促、不重复派发；超时本身不等于失败或取消。
- worker 正常只在完成、真实阻塞或必须立即处理的错误时返回一次，结构为：状态、实际完成范围、关键结论或修改、文件/符号/行号或验证命令证据、未确认项与风险、需主控决定事项。主控按风险抽查决定结论的关键证据，不重复全部调查。
